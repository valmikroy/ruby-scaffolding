# frozen_string_literal: true

module RESTAPINAME
  class V1
    class NS1
      class NS21 < Grape::API
        namespace :ns21 do
          desc <<-HEREDOC
  	Name space 2 1 in v1
          HEREDOC
          get '/' do
            ok 200, 'NS21v1 ' + ENV['EXAMPLE_APP_VAR1']
          end

          get '/only_in_v1' do
            ok 200, 'NS21v1 only in V1'
          end
        end
       end
    end
  end
end
