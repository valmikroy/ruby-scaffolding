# coding: utf-8
lib = File.expand_path('../lib', __FILE__)
$LOAD_PATH.unshift(lib) unless $LOAD_PATH.include?(lib)
require '<%= snake_name %>/version'

Gem::Specification.new do |spec|
spec.name          = '<%= snake_name %>'
spec.version       = <%= camel_name %>::VERSION
spec.authors       = ['git']
spec.email         = ['valmikroy@gmail.com']

spec.summary       = ' <%= camel_name %> ruby command line utility  '
spec.description   = <<-FOO
This is <%= camel_name %> gem command line utility. It uses Thor instance for option parsing and command name creation,
provide logger object under singleton.
FOO
spec.homepage      = 'https://github.com/valmikroy/rubycli-<%= snake_name %>'

# Prevent pushing this gem to RubyGems.org by setting 'allowed_push_host', or
# delete this section to allow pushing this gem to any host.
if spec.respond_to?(:metadata)
spec.metadata['allowed_push_host'] = "TODO: Set to 'http://mygemserver.com'"
else
raise 'RubyGems 2.0 or newer is required to protect against public gem pushes.'
end

spec.files = `git ls-files -z`.split("\x0").reject { |f| f.match(%r{^(test|spec|features)/}) }
spec.executables = spec.files.grep(%r{^bin/}) { |f| File.basename(f) }
spec.require_paths = ['lib']
spec.license = 'MIT'

spec.add_runtime_dependency 'thor', '~> 0.19.1'

spec.add_development_dependency 'bundler', '~> 1.10'
spec.add_development_dependency 'rake', '~> 10.0'
spec.add_development_dependency 'rspec', '~> 3.4'
end